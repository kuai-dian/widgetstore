import { DirectiveBinding } from 'vue';
interface TransferData {
    parentNode: HTMLElement;
    home: Comment;
    hasMovedOut: boolean;
    target: HTMLElement;
}
interface TransferElement extends HTMLElement {
    __transferDomData?: TransferData;
    parentNode: HTMLElement;
}
declare const TransferDom: {
    mounted(el: TransferElement, binding: DirectiveBinding): void;
    updated(el: TransferElement, binding: DirectiveBinding): void;
    unmounted: (el: TransferElement) => void;
};
export default TransferDom;
