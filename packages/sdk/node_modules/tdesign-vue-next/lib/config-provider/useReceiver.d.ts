import { Ref } from 'vue';
export * from './type';
export declare function useReceiver<T>(componentName: string): {
    t: <T>(pattern: T, placement?: Record<string, string | number>) => any;
    global: Ref<T>;
};
