/**
 * tdesign v0.8.0
 * (c) 2022 tdesign
 * @license MIT
 */

import _typeof from '@babel/runtime/helpers/typeof';
import _defineProperty from '@babel/runtime/helpers/defineProperty';
import { isVNode, defineComponent, withDirectives, createVNode, resolveComponent, vShow, resolveDirective } from 'vue';
import get from 'lodash/get';
import { renderContent } from '../utils/render-tnode.js';
import { scrollSelectedIntoView } from '../utils/dom.js';
import { prefix } from '../config.js';
import ClASSNAMES from '../utils/classnames.js';
import Ripple from '../utils/ripple.js';
import props from './option-props.js';
import { Checkbox } from '../checkbox/index.js';
import 'lodash/isEmpty';
import 'lodash/isString';
import 'lodash/isFunction';
import 'lodash/isObject';
import '../_chunks/dep-b66bfe36.js';
import '../utils/easing.js';
import '../utils/set-style.js';
import '../checkbox/checkbox.js';
import '../checkbox/props.js';
import '../utils/event.js';
import '../utils/helper.js';
import '@babel/runtime/helpers/objectWithoutProperties';
import '@babel/runtime/helpers/slicedToArray';
import 'lodash/camelCase';
import '../checkbox/group.js';
import '@babel/runtime/helpers/toConsumableArray';
import 'lodash/intersection';
import '../checkbox/checkbox-group-props.js';
import '../utils/map-props.js';
import 'lodash/kebabCase';
import '../utils/withInstall.js';
import './style/css.js';
import '../checkbox/type.js';

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }

function _isSlot(s) {
  return typeof s === 'function' || Object.prototype.toString.call(s) === '[object Object]' && !isVNode(s);
}

var selectName = "".concat(prefix, "-select");
var _Option = defineComponent({
  name: "TOption",
  components: {
    TCheckbox: Checkbox
  },
  directives: {
    ripple: Ripple
  },
  inject: {
    tSelect: {
      "default": void 0
    }
  },
  props: _objectSpread({}, props),
  data: function data() {
    return {
      isHover: false
    };
  },
  computed: {
    hovering: function hovering() {
      return get(this.tSelect, "hoverOptions[".concat(this.tSelect.hoverIndex, "][").concat(this.tSelect.realValue, "]")) === this.value;
    },
    multiLimitDisabled: function multiLimitDisabled() {
      if (this.tSelect && this.tSelect.multiple && this.tSelect.max) {
        if (this.tSelect.value instanceof Array && this.tSelect.value.indexOf(this.value) === -1 && this.tSelect.max <= this.tSelect.value.length) {
          return true;
        }
      }

      return false;
    },
    classes: function classes() {
      var _ref;

      return ["".concat(prefix, "-select-option"), (_ref = {}, _defineProperty(_ref, ClASSNAMES.STATUS.disabled, this.disabled || this.multiLimitDisabled), _defineProperty(_ref, ClASSNAMES.STATUS.selected, this.selected), _defineProperty(_ref, ClASSNAMES.SIZE[this.tSelect && this.tSelect.size], this.tSelect && this.tSelect.size), _defineProperty(_ref, "".concat(prefix, "-select-option__hover"), this.hovering), _ref)];
    },
    isCreatedOption: function isCreatedOption() {
      return this.tSelect.creatable && this.value === this.tSelect.searchInput;
    },
    show: function show() {
      var _this = this;

      if (!this.tSelect) return false;
      if (this.isCreatedOption) return true;

      if (this.tSelect.canFilter && this.tSelect.searchInput !== "") {
        return this.tSelect.filterOptions.some(function (option) {
          return get(option, _this.tSelect.realValue) === _this.value;
        });
      }

      return true;
    },
    labelText: function labelText() {
      return this.label || String(this.value);
    },
    selected: function selected() {
      var _this2 = this;

      var flag = false;
      if (!this.tSelect) return false;

      if (this.tSelect.value instanceof Array) {
        if (this.tSelect.labelInValue) {
          flag = this.tSelect.value.map(function (item) {
            return get(item, _this2.tSelect.realValue);
          }).indexOf(this.value) !== -1;
        } else {
          flag = this.tSelect.value.indexOf(this.value) !== -1;
        }
      } else if (_typeof(this.tSelect.value) === "object") {
        flag = get(this.tSelect.value, this.tSelect.realValue) === this.value;
      } else {
        flag = this.tSelect.value === this.value;
      }

      return flag;
    }
  },
  watch: {
    value: function value() {
      this.tSelect && this.tSelect.getOptions(this);
    },
    label: function label() {
      this.tSelect && this.tSelect.getOptions(this);
    },
    hovering: function hovering() {
      var _this3 = this;

      if (this.hovering) {
        var timer = setTimeout(function () {
          scrollSelectedIntoView(_this3.tSelect.getOverlayElm(), _this3.$el);
          clearTimeout(timer);
        }, this.tSelect.popupOpenTime);
      }
    }
  },
  mounted: function mounted() {
    this.tSelect && this.tSelect.getOptions(this);
  },
  methods: {
    select: function select(e) {
      e.stopPropagation();

      if (this.disabled || this.multiLimitDisabled) {
        return false;
      }

      var parent = this.$el.parentNode;

      if (parent && parent.className.indexOf("".concat(selectName, "__create-option")) !== -1) {
        this.tSelect && this.tSelect.createOption(this.value.toString());
      }

      this.tSelect && this.tSelect.onOptionClick(this.value, e);
    },
    mouseEvent: function mouseEvent(v) {
      this.isHover = v;
    }
  },
  render: function render() {
    var _this4 = this;

    var classes = this.classes,
        labelText = this.labelText,
        selected = this.selected,
        disabled = this.disabled,
        multiLimitDisabled = this.multiLimitDisabled,
        show = this.show;
    var children = renderContent(this, "default", "content");
    var optionChild = children || labelText;
    return withDirectives(createVNode("li", {
      "class": classes,
      "title": labelText,
      "onMouseenter": function onMouseenter() {
        return _this4.mouseEvent(true);
      },
      "onMouseleave": function onMouseleave() {
        return _this4.mouseEvent(false);
      },
      "onClick": function onClick(e) {
        e.preventDefault();

        _this4.select(e);
      }
    }, [this.tSelect && this.tSelect.multiple ? createVNode(resolveComponent("t-checkbox"), {
      "checked": selected,
      "disabled": disabled || multiLimitDisabled
    }, _isSlot(optionChild) ? optionChild : {
      "default": function _default() {
        return [optionChild];
      }
    }) : optionChild]), [[vShow, show], [resolveDirective("ripple")]]);
  }
});

export { _Option as default };
//# sourceMappingURL=option.js.map
