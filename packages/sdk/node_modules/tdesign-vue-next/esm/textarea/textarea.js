/**
 * tdesign v0.8.0
 * (c) 2022 tdesign
 * @license MIT
 */

import _typeof from '@babel/runtime/helpers/typeof';
import _defineProperty from '@babel/runtime/helpers/defineProperty';
import { defineComponent, createVNode, mergeProps } from 'vue';
import { prefix } from '../config.js';
import ClASSNAMES from '../utils/classnames.js';
import props from './props.js';
import { getCharacterLength } from '../utils/helper.js';
import calcTextareaHeight from './calcTextareaHeight.js';
import { emitEvent } from '../utils/event.js';
import { renderTNodeJSX } from '../utils/render-tnode.js';
import '@babel/runtime/helpers/objectWithoutProperties';
import '@babel/runtime/helpers/slicedToArray';
import 'lodash/camelCase';
import 'lodash/isEmpty';
import 'lodash/isString';
import 'lodash/isFunction';
import 'lodash/isObject';

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }
var name = "".concat(prefix, "-textarea");
var TEXTAREA_WRAP_CLASS = "".concat(prefix, "-textarea__wrap");
var TEXTAREA_TIPS_CLASS = "".concat(prefix, "-textarea__tips");
var TEXTAREA_LIMIT = "".concat(name, "__limit");

function getValidAttrs(obj) {
  var newObj = {};
  Object.keys(obj).forEach(function (key) {
    if (typeof obj[key] !== "undefined") {
      newObj[key] = obj[key];
    }
  });
  return newObj;
}

var _Textarea = defineComponent({
  name: "TTextarea",
  inheritAttrs: false,
  props: _objectSpread({}, props),
  emits: ["keydown", "keyup", "keypress", "focus", "blur", "change", "update:value"],
  data: function data() {
    return {
      focused: false,
      mouseHover: false,
      textareaStyle: {}
    };
  },
  computed: {
    textareaClasses: function textareaClasses() {
      var _ref;

      return [name, (_ref = {}, _defineProperty(_ref, "".concat(prefix, "-is-disabled"), this.disabled), _defineProperty(_ref, "".concat(prefix, "-is-readonly"), this.readonly), _ref)];
    },
    inputAttrs: function inputAttrs() {
      return getValidAttrs({
        autofocus: this.autofocus,
        disabled: this.disabled,
        readonly: this.readonly,
        placeholder: this.placeholder,
        maxlength: this.maxlength || void 0,
        name: this.name || void 0
      });
    },
    characterNumber: function characterNumber() {
      var characterInfo = getCharacterLength(String(this.value));

      if (_typeof(characterInfo) === "object") {
        return characterInfo.length;
      }

      return characterInfo;
    }
  },
  watch: {
    value: function value() {
      this.adjustTextareaHeight();
    }
  },
  mounted: function mounted() {
    this.adjustTextareaHeight();
  },
  methods: {
    adjustTextareaHeight: function adjustTextareaHeight() {
      if (this.autosize === true) {
        this.textareaStyle = calcTextareaHeight(this.$refs.refTextareaElem);
      } else if (_typeof(this.autosize) === "object") {
        var _this$autosize = this.autosize,
            minRows = _this$autosize.minRows,
            maxRows = _this$autosize.maxRows;
        this.textareaStyle = calcTextareaHeight(this.$refs.refTextareaElem, minRows, maxRows);
      } else if (this.$attrs.rows) {
        this.textareaStyle = {
          height: "auto",
          minHeight: "auto"
        };
      }
    },
    focus: function focus() {
      var input = this.$refs.refInputElem;
      input === null || input === void 0 ? void 0 : input.focus();
    },
    blur: function blur() {
      var input = this.$refs.refInputElem;
      input === null || input === void 0 ? void 0 : input.blur();
    },
    handleInput: function handleInput(e) {
      if (e.isComposing || e.inputType === "insertCompositionText") return;
      this.inputValueChangeHandle(e);
    },
    onCompositionend: function onCompositionend(e) {
      this.inputValueChangeHandle(e);
    },
    inputValueChangeHandle: function inputValueChangeHandle(e) {
      var _this = this;

      var target = e.target;
      var val = target.value;

      if (this.maxcharacter && this.maxcharacter >= 0) {
        var stringInfo = getCharacterLength(val, this.maxcharacter);
        val = _typeof(stringInfo) === "object" && stringInfo.characters;
      }

      this.$emit("update:value", val);
      emitEvent(this, "change", val, {
        e: e
      });
      this.$nextTick(function () {
        return _this.setInputValue(val);
      });
      this.adjustTextareaHeight();
    },
    setInputValue: function setInputValue() {
      var v = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "";
      var textareaElem = this.$refs.refTextareaElem;
      var sV = String(v);

      if (!textareaElem) {
        return;
      }

      if (textareaElem.value !== sV) {
        textareaElem.value = sV;
      }
    },
    emitKeyDown: function emitKeyDown(e) {
      if (this.disabled) return;
      emitEvent(this, "keydown", this.value, {
        e: e
      });
    },
    emitKeyUp: function emitKeyUp(e) {
      if (this.disabled) return;
      emitEvent(this, "keyup", this.value, {
        e: e
      });
    },
    emitKeypress: function emitKeypress(e) {
      if (this.disabled) return;
      emitEvent(this, "keypress", this.value, {
        e: e
      });
    },
    emitFocus: function emitFocus(e) {
      if (this.disabled) return;
      this.focused = true;
      emitEvent(this, "focus", this.value, {
        e: e
      });
    },
    emitBlur: function emitBlur(e) {
      this.focused = false;
      emitEvent(this, "blur", this.value, {
        e: e
      });
    }
  },
  render: function render() {
    var _ref2, _String;

    var inputEvents = getValidAttrs({
      onFocus: this.emitFocus,
      onBlur: this.emitBlur,
      onKeydown: this.emitKeyDown,
      onKeyup: this.emitKeyUp,
      onKeypress: this.emitKeypress
    });
    var classes = ["".concat(name, "__inner"), (_ref2 = {}, _defineProperty(_ref2, "".concat(prefix, "-is-").concat(this.status), this.status), _defineProperty(_ref2, ClASSNAMES.STATUS.disabled, this.disabled), _defineProperty(_ref2, ClASSNAMES.STATUS.focused, this.focused), _defineProperty(_ref2, "".concat(prefix, "-resize-none"), this.maxlength), _ref2), "narrow-scrollbar"];

    var textareaNode = createVNode("div", {
      "class": this.textareaClasses
    }, [createVNode("textarea", mergeProps({
      "onInput": this.handleInput,
      "onCompositionend": this.onCompositionend,
      "ref": "refTextareaElem",
      "value": this.value,
      "style": this.textareaStyle,
      "class": classes
    }, this.$attrs, inputEvents, this.inputAttrs), null), this.maxcharacter && createVNode("span", {
      "class": TEXTAREA_LIMIT
    }, ["".concat(this.characterNumber, "/").concat(this.maxcharacter)]), !this.maxcharacter && this.maxlength ? createVNode("span", {
      "class": TEXTAREA_LIMIT
    }, ["".concat(this.value ? (_String = String(this.value)) === null || _String === void 0 ? void 0 : _String.length : 0, "/").concat(this.maxlength)]) : null]);

    var tips = renderTNodeJSX(this, "tips");

    if (tips) {
      return createVNode("div", {
        "class": TEXTAREA_WRAP_CLASS
      }, [textareaNode, createVNode("div", {
        "class": "".concat(TEXTAREA_TIPS_CLASS, " ").concat(prefix, "-textarea__tips--").concat(this.status || "normal")
      }, [tips])]);
    }

    return textareaNode;
  }
});

export { _Textarea as default };
//# sourceMappingURL=textarea.js.map
